import { Main } from "../styles/components/Header";
import { Button } from "./Button";
import { Title } from "./Title";

export function Header() {
    return(
        <Main>
            <div/>
            <Button blue href="/contact">
                Contato
            </Button>
        </Main>
    )
}